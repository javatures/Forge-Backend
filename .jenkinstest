pipeline {
  agent any
  
  
  stages {
    stage('Test') {
      steps {
        sh 'pwd'
        sh 'chmod +x gradlew'
        withGradle {
          sh './gradlew clean test'
        }
      }
    }
  }
}
